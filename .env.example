# Directus instance URL — required in all environments
NUXT_PUBLIC_DIRECTUS_URL=https://your-directus.example.com

# Admin token — only needed locally and for running scripts/seed.mjs
# Do NOT set this in production. The site reads published content via public access.
# DIRECTUS_TOKEN=your_admin_token_here
